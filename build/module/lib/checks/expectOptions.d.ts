/**
 * Determines whether the actual options supplied are expected.
 *
 * Usage:
 *
 * expectOptions(['foo', 'bar'], Object.keys(options));
 * @hidden
 */
export default function expectOptions(expects: string[], actuals: string[]): void;
